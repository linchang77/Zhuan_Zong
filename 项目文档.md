# AirFogSim 前端展示项目文档

## 6.1 系统框架结构

### 6.1.1 总体架构

本系统采用三层架构设计，包括前端展示层、仿真环境层和大模型预测层：

```
┌─────────────────────────────────────────────────────────────┐
│                    前端展示层                                │
│                 airfogsim_gui.py                            │
│            (PyQt5 图形用户界面)                              │
└─────────────────────┬───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                  仿真环境层                                  │
│                simulation_env.py                            │
│           (AirFogSim 环境封装及数据提取)                      │
└─────────────────────┬───────────────────────────────────────┘
                      │
┌─────────────────────▼───────────────────────────────────────┐
│                大模型预测层                                  │
│            LLMsforSR/workflow.py                            │
│              (符号回归公式预测)                               │
└─────────────────────────────────────────────────────────────┘
```

### 6.1.2 核心模块详细说明

#### 前端展示层 ([`airfogsim_gui.py`](airfogsim_gui.py))
- **主要框架**: 基于 PyQt5 开发的图形用户界面
- **核心类**:
  - [`MainWindow`](airfogsim_gui.py:271): 主窗口类，负责整体界面布局和交互
  - [`ConfigDialog`](airfogsim_gui.py:62): 配置对话框，用于修改仿真参数
  - [`SimulationThread`](airfogsim_gui.py:18): 后台线程类，执行仿真和预测任务
  - [`GifViewDialog`](airfogsim_gui.py:203): GIF动画查看对话框，展示可视化指标

#### 仿真环境层 ([`simulation_env.py`](simulation_env.py))
- **主要功能**: 封装 AirFogSim 仿真环境，提供数据提取接口
- **核心类**:
  - [`SimulationEnvironment`](simulation_env.py:14): 仿真环境封装类
  - 集成了车载雾计算环境仿真，支持三种预测模式的数据采集

#### 大模型预测层 ([`LLMsforSR/workflow.py`](LLMsforSR/workflow.py))
- **主要功能**: 基于大语言模型的符号回归预测
- **核心类**:
  - [`AirFogSimWorkflow`](LLMsforSR/workflow.py:18): 工作流封装类，支持多种预测模式和模型

### 6.1.3 配置管理
- **配置文件**: [`examples/config.yaml`](examples/config.yaml) 
- **包含参数**: 仿真时间、车辆数量、各节点算力配置、通信参数等

## 6.2 系统工作流介绍

### 6.2.1 整体工作流程

系统工作流程分为四个主要阶段：

```
用户配置阶段 → 仿真执行阶段 → 公式预测阶段 → 结果展示阶段
```

### 6.2.2 详细工作流程

#### 阶段一：用户配置阶段
1. **模式选择**: 
   - 预测交通流密度
   - 预测任务成功率  
   - 预测计算负载

2. **模型选择**: 
   - claude-sonnet-4-20250514
   - qwen-max-latest
   - gpt-4.1-2025-04-14
   - gpt-4o
   - deepseek-v3-250324

3. **参数配置**: 通过配置对话框修改仿真环境基本参数

#### 阶段二：仿真执行阶段
1. **环境初始化**: 
   - 加载配置文件
   - 创建 AirFogSim 仿真环境
   - 初始化车辆、UAV、RSU等实体

2. **仿真运行**:
   - 执行车载雾计算任务调度仿真
   - 实时采集系统指标数据
   - 根据模式提取相应特征数据

3. **数据保存**:
   - mode1: 每步记录任务成功率
   - mode2: 每5步记录计算延迟和系统状态
   - mode3: 实时记录交通密度数据

#### 阶段三：公式预测阶段
1. **数据处理**: 
   - 根据选择的预测模式加载对应数据集
   - 提取自变量和因变量

2. **大模型调用**:
   - 基于选定的大语言模型进行符号回归
   - 生成数学表达式预测公式

3. **结果优化**:
   - 对生成的公式进行拟合优化
   - 评估预测准确性

#### 阶段四：结果展示阶段
1. **拟合曲线绘制**: 
   - 生成预测值与真实值对比图
   - 展示拟合效果和误差分析

2. **可视化展示**:
   - 实时动图展示仿真过程指标变化
   - 结果图表弹窗显示

### 6.2.3 线程协调机制

系统采用多线程设计确保 UI 响应性：

- **主线程**: 处理 UI 交互和界面更新
- **仿真线程** ([`SimulationThread`](airfogsim_gui.py:18)): 后台执行仿真和预测任务
- **信号槽机制**: 通过 PyQt5 信号实现线程间通信

## 6.3 系统实现功能

### 6.3.1 配置修改功能

#### 功能描述
提供图形化界面修改仿真环境配置参数

#### 支持的配置项
- **仿真参数**: 
  - 模拟时间 (max_simulation_time)
  - 车辆数量 (vehicle_count)
  
- **算力配置**:
  - 车辆CPU算力 (vehicle.cpu)
  - 无人机CPU算力 (uav.cpu) 
  - RSU算力 (rsu.cpu)
  - 云端算力 (cloud.cpu)

#### 实现方式
- 通过 [`ConfigDialog`](airfogsim_gui.py:62) 类实现
- 支持参数验证和错误提示
- 配置修改实时生效

### 6.3.2 模式选择功能

#### 支持的预测模式

**1. 预测交通流密度模式**
- **数据源**: [`LLMsforSR/datasets/next_step_density.csv`](LLMsforSR/datasets/next_step_density.csv)
- **自变量**: 前一时刻车辆密度、车辆平均速度、V2U通信速率、V2I通信速率
- **因变量**: 当前时刻车辆密度
- **应用场景**: 交通流量预测和道路拥堵分析

**2. 预测任务成功率模式**  
- **数据源**: [`LLMsforSR/datasets/Airfogsim_time_1s.csv`](LLMsforSR/datasets/Airfogsim_time_1s.csv)
- **自变量**: 车辆平均速度、UAV平均速度、通信速率、计算延迟、前一时刻成功率
- **因变量**: 任务完成成功率
- **应用场景**: 车载雾计算性能优化

**3. 预测计算负载模式**
- **数据源**: [`LLMsforSR/datasets/airfogsim_for_cd.csv`](LLMsforSR/datasets/airfogsim_for_cd.csv) 
- **自变量**: 通信速率、任务队列状态（等待、卸载、计算、完成、失败）
- **因变量**: 平均计算延迟
- **应用场景**: 计算资源调度优化

### 6.3.3 大模型选择功能

#### 支持的预训练模型
- **Claude Sonnet**: claude-sonnet-4-20250514 (默认)
- **通义千问**: qwen-max-latest  
- **GPT系列**: gpt-4.1-2025-04-14, gpt-4o
- **DeepSeek**: deepseek-v3-250324

#### 模型配置参数
- **生成数量 (N)**: 每次调用生成表达式数量 (默认3)
- **温度参数 (temp)**: 控制生成多样性 (默认0.7)
- **迭代次数**: 符号回归优化轮数 (默认1)

### 6.3.4 数据提取可视化功能

#### 实时数据监控
- **任务状态**: 等待、卸载、计算、完成、失败任务数量
- **通信指标**: V2U、V2I、U2I 通信速率
- **移动性指标**: 车辆和UAV平均速度
- **计算指标**: 平均计算延迟

#### 可视化展示方式
1. **实时动图**: 基于 [`live_plot.py`](live_plot.py) 的 MultiLivePlot 类
2. **历史动图查看**: 通过 [`GifViewDialog`](airfogsim_gui.py:203) 查看已保存的指标动图
3. **拟合曲线图**: 显示预测公式与真实数据的拟合效果

### 6.3.5 功能集成特性

#### 端到端工作流
- **一键执行**: 从仿真数据提取到公式预测的完整流程
- **自动化处理**: 数据预处理、模型调用、结果生成全自动化
- **错误处理**: 完善的异常捕获和错误提示机制

#### 结果管理
- **时间戳目录**: 每次运行创建独立的结果目录
- **详细日志**: 完整记录执行过程和中间结果  
- **成本统计**: 记录大模型调用成本
- **多格式输出**: 支持JSON、图像、GIF等多种结果格式

#### 用户体验优化
- **进度反馈**: 实时显示执行进度和状态信息
- **后台执行**: 仿真和预测在后台线程运行，避免界面冻结
- **快速查看**: 支持快速查看历史结果和可视化图表
- **操作指导**: 清晰的界面布局和操作提示

### 6.3.6 扩展性设计

#### 模式扩展
- **模块化设计**: 新增预测模式只需添加相应的数据处理和prompt配置
- **数据集适配**: 支持自定义数据集和特征变量配置

#### 模型扩展  
- **统一接口**: 通过标准化接口支持新的大语言模型
- **参数配置**: 灵活的模型参数配置机制

这个系统成功地将车载雾计算仿真、大模型符号回归和可视化展示集成为一个完整的预测分析平台，为车载雾计算研究提供了强有力的工具支持。


