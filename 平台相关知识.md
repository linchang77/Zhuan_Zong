是的，AirFogSim 仿真平台是以 `simulation_interval` 为单位进行离散时间步模拟的。每经过一个 `simulation_interval`，仿真会进行一次“系统状态更新 + 算法调度”的循环。

具体来说，在每个 `simulation_interval` 内部，系统会完成以下几件事：

1. **环境状态更新（如任务生成、UAV移动、网络拓扑变化等）**
2. **算法调度（执行 scheduleStep()）**
   - 根据用户实现的算法模块（如任务卸载、通信资源分配、计算资源分配、UAV轨迹优化等）依次调用相关函数：
     - `scheduleOffloading()`
     - `scheduleCommunication()`
     - `scheduleComputing()`
     - `scheduleUAV()`
3. **系统根据调度决策执行实际的任务调度、资源分配等操作，并记录仿真结果**

所以，如果你设置了 `simulation_interval=1.0`，那么系统每 1.0 秒仿真时间就执行一次上述流程。

---

如果你在调试算法时，需要注意：
- 在 `AlgorithmModule.scheduleStep()` 这个函数中，就是你在每个时间步“控制世界”的机会。
- 可以通过 `environment.getSimulationTime()` 获取当前仿真的时间戳，来做与时间有关的控制逻辑。

- **个人理解：每个时间间隔运行一遍任务调度，卸载等流程**

